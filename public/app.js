const BOARD_INCHES = 36;
const board = document.querySelector("#board"), tabletop = board.parentElement, roomEl = document.querySelector("#room"), status = document.querySelector("#status"), statusDot = document.querySelector("#status-dot"), players = document.querySelector("#players"), selectionPanel = document.querySelector("#selection-panel"), selectedModel = document.querySelector("#selected-model"), modelDetails = document.querySelector("#model-details"), rotationValue = document.querySelector("#rotation-value"), zoomLabel = document.querySelector("#zoom-label"), measureToggle = document.querySelector("#measure-toggle"), clearMeasure = document.querySelector("#clear-measure"), rangeToggle = document.querySelector("#range-toggle");
let attempt = 0, models = [], selectedId = null, socket, measureMode = false, measurement = null, rangeVisible = false;
const view = { x: 0, y: 0, zoom: 1 }, requestedRoom = new URL(location.href).searchParams.get("room");

function updateConnection(label, isOnline) { status.textContent = label; statusDot.classList.toggle("online", isOnline); }
function fitBoard() { const size = Math.floor(Math.min(tabletop.clientWidth, tabletop.clientHeight)); if (size > 0) board.style.setProperty("--board-size", `${size}px`); }
function selected() { return models.find((entry) => entry.id === selectedId); }
function send(message) { if (socket?.readyState === WebSocket.OPEN) socket.send(JSON.stringify(message)); }
function acceptModels(incoming) { models = incoming.map((model) => { const previous = models.find((entry) => entry.id === model.id); return { ...model, rotation: Number.isFinite(model.rotation) ? model.rotation : previous?.rotation ?? 0 }; }); }
function updateSelection() { const model = selected(); selectionPanel.hidden = !model; rangeToggle.disabled = !model; if (!model) { rangeVisible = false; rangeToggle.classList.remove("active"); return; } selectedModel.textContent = model.name; modelDetails.textContent = `Position ${model.x.toFixed(1)}, ${model.y.toFixed(1)}`; rotationValue.value = `${Math.round(model.rotation ?? 0)}°`; rotationValue.textContent = rotationValue.value; }
function applyView() { board.style.transform = `translate(${view.x}px, ${view.y}px) scale(${view.zoom})`; zoomLabel.value = `${Math.round(view.zoom * 100)}%`; zoomLabel.textContent = zoomLabel.value; }
function setZoom(nextZoom, clientX, clientY) { const zoom = Math.max(0.6, Math.min(2.5, nextZoom)), rect = board.parentElement.getBoundingClientRect(), dx = (clientX ?? rect.left + rect.width / 2) - rect.left - rect.width / 2, dy = (clientY ?? rect.top + rect.height / 2) - rect.top - rect.height / 2; view.x = dx - ((dx - view.x) * zoom) / view.zoom; view.y = dy - ((dy - view.y) * zoom) / view.zoom; view.zoom = zoom; applyView(); }
function resetView() { Object.assign(view, { x: 0, y: 0, zoom: 1 }); applyView(); }
function measurementDistance() { if (!measurement) return 0; return Math.hypot(measurement.end.x - measurement.start.x, measurement.end.y - measurement.start.y) * BOARD_INCHES / 100; }
function measurementElement() { if (!measurement) return null; const { start, end } = measurement, midX = (start.x + end.x) / 2, midY = (start.y + end.y) / 2, ruler = document.createElement("div"); ruler.className = "measurement"; ruler.innerHTML = `<svg viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true"><line x1="${start.x}" y1="${start.y}" x2="${end.x}" y2="${end.y}"/><circle cx="${start.x}" cy="${start.y}" r="1.1"/><circle cx="${end.x}" cy="${end.y}" r="1.1"/></svg><output style="left:${midX}%;top:${midY}%">${measurementDistance().toFixed(1)}″</output>`; return ruler; }
function rangeElement() { const model = selected(); if (!rangeVisible || !model) return null; const diameter = 4 / BOARD_INCHES * 100, circle = document.createElement("div"); circle.className = "range-circle"; circle.style.cssText = `left:${model.x}%;top:${model.y}%;width:${diameter}%;height:${diameter}%`; circle.innerHTML = "<span>2″</span>"; return circle; }
function render() { const elements = []; const range = rangeElement(); if (range) elements.push(range); for (const model of models) { const token = document.createElement("button"); token.className = `model ${selectedId === model.id ? "selected" : ""}`; token.style.cssText = `left:${model.x}%;top:${model.y}%;--model-color:${model.color};--rotation:${model.rotation ?? 0}deg`; token.dataset.id = model.id; token.setAttribute("aria-label", `Move ${model.name}`); token.setAttribute("aria-pressed", String(selectedId === model.id)); token.innerHTML = `<span>${model.name.slice(0, 1)}</span><em>${model.name}</em>`; token.addEventListener("pointerdown", startDrag); elements.push(token); } const ruler = measurementElement(); if (ruler) elements.push(ruler); board.replaceChildren(...elements); updateSelection(); clearMeasure.hidden = !measurement; }
function moveSelected(dx, dy) { const model = selected(); if (!model) return; model.x = Math.max(2, Math.min(98, model.x + dx)); model.y = Math.max(2, Math.min(98, model.y + dy)); send({ type: "move", id: model.id, rotation: model.rotation, x: model.x, y: model.y }); render(); }
function moveSelectedAlongFacing(distance) { const model = selected(); if (!model) return; const radians = ((Number.isFinite(model.rotation) ? model.rotation : 0) * Math.PI) / 180; moveSelected(Math.sin(radians) * distance, -Math.cos(radians) * distance); }
function rotateSelected(degrees) { const model = selected(); if (!model) return; const currentRotation = Number.isFinite(model.rotation) ? model.rotation : 0; model.rotation = ((currentRotation + degrees) % 360 + 360) % 360; send({ type: "rotate", id: model.id, rotation: model.rotation }); render(); }
function pointAt(event) { const rect = board.getBoundingClientRect(); return { x: Math.max(0, Math.min(100, (event.clientX - rect.left) / rect.width * 100)), y: Math.max(0, Math.min(100, (event.clientY - rect.top) / rect.height * 100)) }; }
function setMeasureMode(active) { measureMode = active; measureToggle.classList.toggle("active", active); measureToggle.textContent = active ? "Draw ruler" : "Measure"; board.classList.toggle("measuring", active); }
function startMeasurement(event) { event.preventDefault(); event.stopPropagation(); const start = pointAt(event); measurement = { start, end: start }; const move = (pointer) => { measurement.end = pointAt(pointer); render(); }; const end = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", end); setMeasureMode(false); render(); }; window.addEventListener("pointermove", move); window.addEventListener("pointerup", end, { once: true }); render(); }
function startDrag(event) { if (measureMode) return startMeasurement(event); if (socket?.readyState !== WebSocket.OPEN) return; event.stopPropagation(); selectedId = event.currentTarget.dataset.id; const move = (pointer) => { const rect = board.getBoundingClientRect(), model = selected(); model.x = Math.max(2, Math.min(98, ((pointer.clientX - rect.left) / rect.width) * 100)); model.y = Math.max(2, Math.min(98, ((pointer.clientY - rect.top) / rect.height) * 100)); render(); }; const end = () => { const model = selected(); if (model) send({ type: "move", id: model.id, rotation: model.rotation, x: model.x, y: model.y }); window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", end); }; window.addEventListener("pointermove", move); window.addEventListener("pointerup", end, { once: true }); render(); }
board.addEventListener("pointerdown", (event) => { if (measureMode) return startMeasurement(event); if (event.target !== board || event.button !== 0) return; const start = { x: event.clientX, y: event.clientY, viewX: view.x, viewY: view.y }; board.classList.add("panning"); board.setPointerCapture(event.pointerId); const pan = (pointer) => { view.x = start.viewX + pointer.clientX - start.x; view.y = start.viewY + pointer.clientY - start.y; applyView(); }; const end = () => { board.classList.remove("panning"); board.removeEventListener("pointermove", pan); board.removeEventListener("pointerup", end); }; board.addEventListener("pointermove", pan); board.addEventListener("pointerup", end, { once: true }); });
board.addEventListener("wheel", (event) => { event.preventDefault(); setZoom(view.zoom * (event.deltaY < 0 ? 1.1 : 0.9), event.clientX, event.clientY); }, { passive: false });
window.addEventListener("keydown", (event) => { if (event.key.toLowerCase() === "m" && !event.metaKey && !event.ctrlKey && !event.altKey) { event.preventDefault(); setMeasureMode(!measureMode); return; } if (!selected() || event.metaKey || event.ctrlKey || event.altKey || event.target.matches("input, textarea, select")) return; const step = event.shiftKey ? 5 : 1, actions = { ArrowUp: () => moveSelectedAlongFacing(step), ArrowDown: () => moveSelectedAlongFacing(-step), ArrowLeft: () => rotateSelected(-15), ArrowRight: () => rotateSelected(15), "[": () => rotateSelected(-15), "]": () => rotateSelected(15), r: () => rotateSelected(-(selected().rotation ?? 0)), Escape: () => { selectedId = null; render(); } }; if (!actions[event.key]) return; event.preventDefault(); actions[event.key](); });
function connect() { updateConnection(attempt ? "Reconnecting…" : "Connecting…", false); const protocol = location.protocol === "https:" ? "wss" : "ws"; socket = new WebSocket(`${protocol}://${location.host}/socket${requestedRoom ? `?room=${encodeURIComponent(requestedRoom)}` : ""}`); socket.addEventListener("open", () => { attempt = 0; updateConnection("Live board", true); }); socket.addEventListener("close", () => { updateConnection("Reconnecting…", false); window.setTimeout(connect, Math.min(8000, 500 * 2 ** attempt++)); }); socket.addEventListener("message", ({ data }) => { const message = JSON.parse(data); if (message.type === "room") { roomEl.textContent = message.roomId.slice(0, 8).toUpperCase(); history.replaceState({}, "", `?room=${message.roomId}`); acceptModels(message.models); render(); } if (message.type === "state") { acceptModels(message.models); render(); } if (message.type === "presence") players.textContent = message.count === 1 ? "Just you" : `${message.count} players connected`; }); }
document.querySelector("#zoom-in").addEventListener("click", () => setZoom(view.zoom * 1.2)); document.querySelector("#zoom-out").addEventListener("click", () => setZoom(view.zoom / 1.2)); document.querySelector("#reset-view").addEventListener("click", resetView); measureToggle.addEventListener("click", () => setMeasureMode(!measureMode)); clearMeasure.addEventListener("click", () => { measurement = null; render(); }); rangeToggle.addEventListener("click", () => { rangeVisible = !rangeVisible; rangeToggle.classList.toggle("active", rangeVisible); render(); }); document.querySelector("#rotate-left").addEventListener("click", () => rotateSelected(-15)); document.querySelector("#rotate-right").addEventListener("click", () => rotateSelected(15)); document.querySelector("#copy-link").addEventListener("click", async () => { await navigator.clipboard.writeText(location.href); document.querySelector("#copy-link").textContent = "Invite link copied"; window.setTimeout(() => { document.querySelector("#copy-link").textContent = "Copy invite link"; }, 1600); });
new ResizeObserver(fitBoard).observe(tabletop);
requestAnimationFrame(fitBoard);
applyView(); connect();
