const board=document.querySelector('#board'),roomEl=document.querySelector('#room'),status=document.querySelector('#status'),dot=document.querySelector('#status-dot'),players=document.querySelector('#players');let models=[],selectedId=null;const requestedRoom=new URL(location.href).searchParams.get('room'),socket=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/socket${requestedRoom?`?room=${encodeURIComponent(requestedRoom)}`:''}`);function render(){board.replaceChildren(...models.map(model=>{const token=document.createElement('button');token.className=`model ${selectedId===model.id?'selected':''}`;token.style.cssText=`left:${model.x}%;top:${model.y}%;--model-color:${model.color}`;token.dataset.id=model.id;token.setAttribute('aria-label',`Move ${model.name}`);token.innerHTML=`<span>${model.name.slice(0,1)}</span><em>${model.name}</em>`;token.addEventListener('pointerdown',startDrag);return token}))}function startDrag(event){const token=event.currentTarget;selectedId=token.dataset.id;const move=pointer=>{const rect=board.getBoundingClientRect(),model=models.find(entry=>entry.id===selectedId);model.x=Math.max(2,Math.min(98,(pointer.clientX-rect.left)/rect.width*100));model.y=Math.max(2,Math.min(98,(pointer.clientY-rect.top)/rect.height*100));render()};const end=()=>{const model=models.find(entry=>entry.id===selectedId);socket.send(JSON.stringify({type:'move',id:model.id,x:model.x,y:model.y}));window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',end)};window.addEventListener('pointermove',move);window.addEventListener('pointerup',end,{once:true});render()}socket.addEventListener('open',()=>{status.textContent='Live board';dot.classList.add('online')});socket.addEventListener('close',()=>{status.textContent='Disconnected';dot.classList.remove('online')});socket.addEventListener('message',({data})=>{const message=JSON.parse(data);if(message.type==='room'){roomEl.textContent=message.roomId.slice(0,8).toUpperCase();history.replaceState({},'',`?room=${message.roomId}`);models=message.models;render()}if(message.type==='state'){models=message.models;render()}if(message.type==='presence')players.textContent=message.count===1?'Just you':`${message.count} players connected`});document.querySelector('#copy-link').addEventListener('click',async()=>{await navigator.clipboard.writeText(location.href);document.querySelector('#copy-link').textContent='Invite link copied';setTimeout(()=>document.querySelector('#copy-link').textContent='Copy invite link',1600)});
